package test;

import java.io.IOException;

public class Experiment {
    
    public static void main(String[] args) throws IOException {
        // String path = "./experiment/abstract/unlabeledCorpus.txt";
        // BufferedReader br = new BufferedReader(new InputStreamReader(
        // new FileInputStream(path), "UTF-8"));

        System.out.println("你好".length());
//        String s = "测量 脉冲 A ② S0 MHZ -LRB-";
//        System.out.println(s);
//        System.out.println(s.replaceAll("[^\\p{L}]|[A-Za-z0-9]+", " "));
        // assert m.find();
        // System.out.println(m.group());
        // final String PATTERN = "(-LRB-)|(-RRB-)|\\w+|\\d+";
        // BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(
        // new FileOutputStream("./experiment/abstract/newUnlabeledCorpus.txt"),
        // "UTF-8"));
        //
        // while (br.ready()) {
        // String line = br.readLine();
        // bw.append(line.replaceAll(PATTERN, ""));
        // bw.newLine();
        // }
        // bw.close();br.close();
    }

}
